{
  "name": "@tdurieux/log-utils",
  "version": "0.0.4",
  "description": "Library that allows to clean and parse log files.",
  "main": "lib/index.js",
  "types": "./lib/index.d.ts",
  "type": "module",
  "author": "Thomas Durieux",
  "repository": {
    "type": "git",
    "url": "https://github.com/tdurieux/log-utils"
  },
  "license": "MIT",
  "scripts": {
    "test": "jest --coverage=true",
    "build": "npm run clean && npm run build:lib && npm run build:doc && npm run browserify",
    "build:lib": "tsc",
    "build:doc": "typedoc --out docs/doc --entryPointStrategy expand ./src",
    "clean": "rm -rf lib docs/doc",
    "browserify": "tsc && browserify -p esmify lib/index.js --standalone logUtil -o docs/js/logUtil.js"
  },
  "dependencies": {
    "ansi-regex": "^6.0.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "browserify": "^17.0.0",
    "codecov": "^3.8.3",
    "esmify": "^2.1.1",
    "jest": "^29.5.0",
    "ts-jest": "^29.0.5",
    "typedoc": "^0.23.28",
    "typescript": "^5.0.3"
  }
}
